# Docker-specific environment variable overrides
# These override the defaults in .env.example when running inside Docker

# Use Docker service name instead of localhost
MONGODB_URI=mongodb://mongo:27017/terras

# Pipeline download directory (same value, explicit for container clarity)
GENT3D_DOWNLOAD_DIR=./data/gent3d
